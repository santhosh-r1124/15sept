[project]
name = "legal-platform-notifications"
version = "0.0.0"
description = "Notification fan-out — email / SMS / in-app (Phase 11)"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pytest>=8.3.4", "ruff>=0.8.4", "mypy>=1.14.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["notifications"]
